import LayoutStep from './LayoutStep.svelte'

// --------------------------------------------

describe('LayoutStep', () => {
  it('renders', () => {
    render(LayoutStep, { currentRoute: '' })
  })
})
