import SubmitCharge from './SubmitCharge.svelte'

// --------------------------------------------

describe('SubmitCharge', () => {
  it('renders', () => {
    render(SubmitCharge, { account: {} })
  })
})
