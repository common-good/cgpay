.page
  height 100%
  display flex
  flex-direction column
  align-items center
  justify-content space-between

.page-title
  text(xl)
  font-weight 600
  margin-bottom $s0
  text-align center

.top
  width 100%
  height 100%
  display flex
  flex-direction column
  align-items center

.bottom
  width 100%
  text-align center

.actions
  display flex
  justify-content space-between
  margin-top $s0
