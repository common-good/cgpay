.page
  display flex
  flex-direction column
  align-items center
  justify-content space-between
  flex-grow 1
  overflow auto

.page-title
  text(xl)
  font-weight 600
  margin-bottom $s-2
  text-align center

.top
  flex 1 0 0
  display flex
  flex-direction column
  overflow scroll
  width 100%
  
.bottom
  flex 0 0 20px
  width 100%
  display flex
  align-items center
  justify-content space-between
  margin-top $s0
