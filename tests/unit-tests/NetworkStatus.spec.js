import NetworkStatus from '#modules/NetworkStatus.svelte'

// --------------------------------------------

describe('NetworkStatus', () => {
  it('renders', () => {
    render(NetworkStatus)
  })
})
