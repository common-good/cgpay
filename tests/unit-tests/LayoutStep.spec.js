import LayoutStep from '#modules/Layout.svelte'

// --------------------------------------------

describe('LayoutStep', () => {
  it('renders', () => {
    render(LayoutStep, { currentRoute: '' })
  })
})
