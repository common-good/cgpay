import LayoutStep from '#modules/LayoutStep.svelte'

// --------------------------------------------

describe('LayoutStep', () => {
  it('renders', () => {
    render(LayoutStep, { currentRoute: '' })
  })
})
